% =============================================================================
%  lemma_88_short.tex
%
%  STANDALONE REVIEW COPY of a SHORT proof of the extension lemma
%  (lemma:ExtensionOfTamelyRamifiedSheaf in the thesis, currently Lemma 88):
%
%      A locally constant sheaf on U^{(d)}, tamely ramified along the
%      boundary divisor H of Takeuchi's compactification, extends to a
%      locally constant sheaf on the whole compactification.
%
%  This is a THIRD proof, drafted to be as short as possible, modelled on
%  how Deligne's tame case is finished in Tendler's notes
%  (arXiv:1507.00104, last section) and in Guignard's paper: instead of
%  computing valuations on the blowup (extension_lemma_completion/) or
%  counting degrees on the generic fiber (lemma_88_route_2/), it uses the
%  two soft facts
%      (i)  the generic fiber of the OPEN Abel--Jacobi map Phi_d is an
%           honest affine space A^N over the function field kappa of
%           Pic^d_{C,m}, containing eta_0 in its closure with the same
%           local ring, and
%      (ii) affine space has no nontrivial abelian covers of degree prime
%           to p (pi_1^{ab}(A^N) is pro-p) -- five lines of Kummer theory.
%  The prime-to-p inertia at eta_0 then dies because the torsor is
%  CONSTANT on the generic fiber, the p-primary inertia dies by tameness
%  alone, and Zariski--Nagata purity converts "unramified at eta_0" into
%  the extension. This realizes "Route 1" of the red placeholder note
%  (the kernel of pi_1^{ab}(U^{(d)}) -> pi_1^{ab}(Pic) meets our torsor
%  in its pro-p part only).
%
%  The proof body lives in
%      lemma_88_short/lemma_88_short_body.tex
%  and is fully self-contained: it does not depend on the other two proof
%  drafts, and all labels it defines carry the suffix "_short" (resp.
%  "Short" for the lemma itself), so the body can alternatively be \input
%  into the thesis after ProofOfReducedTheorem.tex without label clashes.
%
%  Cross-references to thesis results (Takeuchi's theorem, the degree
%  condition, the tameness definitions, etc.) are resolved against the
%  thesis's main.aux via the xr package, so their numbering below matches
%  the compiled thesis (main.pdf).
%
%  Compile (from inside this directory):
%      TEXINPUTS=..: BIBINPUTS=..: latexmk -pdf lemma_88_short.tex
% =============================================================================
\documentclass[11pt]{article}
\usepackage{geometry}\geometry{margin=1in}
\usepackage[T1]{fontenc}
\usepackage{ifthen}

\setlength{\parindent}{1em}
\setlength{\parskip}{0.5em}

\input{includes}
\input{MacrosAndFigures}

\usepackage{csquotes}
\usepackage[
backend=biber,
style=alphabetic,
sorting=ynt
]{biblatex}
\addbibresource{Bib.bib}

\newboolean{ThesisIntro}
\setboolean{ThesisIntro}{false}

% Resolve references to thesis-internal results against the thesis aux file.
\usepackage{xr}
\externaldocument{../main}

\title{A short proof of
\texorpdfstring{\Cref{lemma:ExtensionOfTamelyRamifiedSheaf}}{the extension lemma},\\
after Deligne's tame case\\
\large (the generic Abel--Jacobi fiber is an affine space, and affine space\\
has no prime-to-$p$ abelian covers)}
\author{}
\date{\today}

\begin{document}
\maketitle

\noindent\textbf{Goal.}
The proof of \Cref{thm:GCFT_reduced} (``Theorem 2'') reduces, in the ramified
case $\mdls > 0$, to the extension lemma whose proof in the thesis is
currently a placeholder (\texttt{ProofOfReducedTheorem.tex}): a locally
constant sheaf $\cF^{[d]}$ on $U^{(d)}$ which is \emph{tamely} ramified along
the boundary divisor $H = \Cmod{d} \setminus U^{(d)}$ extends to a locally
constant sheaf on all of $\Cmod{d}$.

This note gives a proof modelled on the way Deligne's \emph{tame} case of
geometric class field theory is finished in
(\cite{tendler2015geometricclassfieldtheory}, last section) and in
(\cite{Guignard2018}, Section 5): there, after the unramified case, the tame
case is deduced from two soft facts --- the fibers of the open Abel--Jacobi
map are affine spaces, and the abelianized fundamental group of affine space
is pro-$p$, so a character of prime-to-$p$ order is automatically constant
on the fibers. The wild case cannot be reduced to the unramified one on the
curve itself (no prime-to-$p$ power of a wild character becomes unramified),
which is why the thesis needs Takeuchi's compactification and the tameness
theorem. But \emph{at the boundary point $\eta_0$} the same two soft facts
apply verbatim and finish the proof: the prime-to-$p$ inertia dies because
the torsor is constant on the generic fiber of $\Phi_d$, the $p$-primary
inertia dies by tameness alone, and purity of the branch locus converts
``unramified at $\eta_0$'' into the extension. This realizes \textbf{Route 1}
of the placeholder note: the kernel of
$\pi_1^{ab}(U^{(d)}) \to \pi_1^{ab}(\PicCm[d])$ can meet our torsor only in
its pro-$p$ part.

All numbered references to results \emph{not} proved in this note
(Takeuchi's compactification, the degree condition, the tameness
definitions, purity inputs, etc.) carry the numbering of the compiled thesis
(\texttt{main.pdf}).

\bigskip
\hrule
\bigskip

\input{lemma_88_short/lemma_88_short_body.tex}

\printbibliography
\end{document}
